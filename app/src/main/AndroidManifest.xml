<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.agoni.java" >
    
        <!-- 读取您的SD卡中的内容 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- 修改或删除您的SD卡中的内容 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <!-- 读取您共享存储空间中的内容 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
        <!-- 所有文件访问权限 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    
     <!-- 完全的网络访问权限 -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 查看网络连接 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:name="com.agoni.util.crash.CrashApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        android:resizeableActivity="true">
        <!-- android:name="com.agoni.util.crash.CrashApplication" 这一个是crash崩溃日志功能-->
        <!--安卓11开始必须添加 所有文件访问权限 android:requestLegacyExternalStorage="true"-->
        <!--使用 android:theme="@style/AppTheme.Translucent" 可隐藏默认Toolbar导航栏-->
		
        <activity
            android:exported="true"
            android:name="com.agoni.java.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        
        <!-- 如果你啥都不懂 不要动下面 你可以删除 -->
        <!-- 下面崩溃日志功能界面如果你删除了 java文件夹的io 把这里和上面application的name也一并删除了不然会闪退 -->
        <activity
            android:name="com.agoni.util.crash.CrashActivity"
            android:exported="false"
            android:process=":crash" />
            <!-- 分享崩溃截图必要 请勿删除 如果使用取消注释 -->
            <!-- 将app/src/main/java/com/agoni/util/crash/CrashActivity.java文件的注释也一并删除-->
            <!--provider
				android:name="android.support.v4.content.FileProvider"
				android:authorities="${applicationId}.fileprovider"
				android:exported="false"
				android:grantUriPermissions="true">
				<meta-data
					android:name="android.support.FILE_PROVIDER_PATHS"
					android:resource="@xml/file_paths" />
			</provider-->
    </application>

</manifest>
